{
  "name": "the-brain-room",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build:frontend",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "echo 'No backend yet — skipping backend build.'"
  }
}